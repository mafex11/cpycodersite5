module.exports = {
    theme: {
      fontFamily: {
        sans: [
          '"DM Sans"',
          'sans-serif',
          // Fallback fonts if needed
          { fontFeatureSettings: '"cv11", "ss01"' }
        ],
        // Keep your other font families
      },
      // Rest of your Tailwind config
    }
  }